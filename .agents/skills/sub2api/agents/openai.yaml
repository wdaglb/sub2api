interface:
  display_name: "Sub2api"
  short_description: "Repo-specific patterns and workflows for sub2api"
  default_prompt: "Use the sub2api repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true